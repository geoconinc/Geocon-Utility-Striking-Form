# Copy to .env for local dev. On Netlify, set these in Site settings → Environment variables.

SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
EMAIL_TO=
LEGAL_EMAIL=
PORT=3000
